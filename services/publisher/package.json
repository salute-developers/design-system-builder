{
  "name": "publisher",
  "version": "0.1.0",
  "description": "",
  "main": "index.js",
  "bin": {
    "publisher": "dist/publisher.js"
  },
  "scripts": {
    "start": "node app/server.js",
    "postbuild": "chmod +x app/cli.js",
    "build": "esbuild app/cli.js --bundle --platform=node --format=cjs --outfile=dist/publisher.js && chmod +x dist/publisher.js"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "dependencies": {
    "commander": "^12.1.0",
    "express": "^5.1.0",
    "fs-extra": "^11.3.2",
    "multer": "^2.0.2",
    "npm-registry-client": "^8.6.0",
    "tar": "^7.5.1"
  },
  "devDependencies": {
    "esbuild": "^0.24.0"
  }
}
