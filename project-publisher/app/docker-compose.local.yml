services:
  backend:
    environment:
      ANDROID_COMPOSE_RUNNER: plasma/android-runner-compose:dev
      ANDROID_XML_RUNNER: plasma/android-runner:dev
      WEB_RUNNER: plasma/web-runner:dev
  db:
    ports:
      - "5433:5432"