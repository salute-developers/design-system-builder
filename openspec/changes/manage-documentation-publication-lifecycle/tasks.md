## 1. Схема данных и конфигурация

- [ ] 1.1 Добавить миграцию `publication_cleanup_jobs` с внешним ключом `publication_id`, `ON DELETE CASCADE`, полями состояния и аренды, а также индексом захвата готовых заданий.
- [ ] 1.2 Добавить `documentation_bundles.storage_deleted_at` и создать ровно по одному заданию для существующих публикаций `superseded` с `eligible_at` через 24 часа от времени миграции.
- [ ] 1.3 Добавить настройки `documentation.cleanup.*` с проверкой положительных интервалов, `enabled=false` и `graceSeconds=86400` по умолчанию.
- [ ] 1.4 Покрыть миграцию проверками внешнего ключа, каскадного удаления, повторного запуска и заполнения существующих данных.

## 2. Доменный и прикладной слои

- [ ] 2.1 Добавить `PublicationCleanupPolicy`, `PublicationCleanupClaim`, `PublicationCleanupTarget`, `PublicationCleanupRunResult` и безопасные классы ошибок без инфраструктурных типов.
- [ ] 2.2 Добавить порты `PublicationCleanupRepository`, `PublicationObjectDeleter` и `PublicationLifecycleMetrics`.
- [ ] 2.3 Реализовать `CleanupSupersededPublicationUseCase`: захват, проверка активного указателя, удаление объектов, завершающая транзакция, повтор и блокировка.
- [ ] 2.4 Изменить транзакцию публикации так, чтобы она одновременно переводила прежний снимок в `superseded` и создавала для него задание с настроенной защитной задержкой.
- [ ] 2.5 Добавить модульные тесты первой и повторной публикации, потери аренды, частичного удаления, временных ошибок, нарушения инварианта и защиты других версий.

## 3. Адаптеры PostgreSQL и S3

- [ ] 3.1 Реализовать конкурентный захват через `FOR UPDATE SKIP LOCKED`, продление аренды и условные переходы состояния по владельцу аренды.
- [ ] 3.2 Реализовать выборку только точных ключей объектов публикации и исходного пакета; запретить удаление по префиксу.
- [ ] 3.3 Реализовать пакетное идемпотентное удаление S3, где отсутствующий объект считается успешным результатом.
- [ ] 3.4 Реализовать завершающую транзакцию с повторной проверкой активного указателя, удалением публикации, каскадным удалением задания и заполнением `storage_deleted_at`.
- [ ] 3.5 Добавить интеграционные тесты конкурентного захвата, истечения аренды, каскадного удаления и повтора после сбоя между S3 и PostgreSQL.

## 4. Фоновый цикл и наблюдаемость

- [ ] 4.1 Подключить периодический фоновый цикл в `app` и `di`, учитывать `documentation.cleanup.enabled` и корректно завершать текущую попытку при остановке приложения.
- [ ] 4.2 Реализовать `PublicationLifecycleMetrics` с ограниченными значениями результата и класса ошибки, без идентификаторов в метках.
- [ ] 4.3 Добавить структурированные журналы переходов, размер очереди, возраст старейшего готового задания, число истёкших аренд, длительность попытки и удалённые байты.
- [ ] 4.4 Убедиться, что состояние очистки доступно внутренней диагностике, не добавляет HTTP-маршрут и не влияет на `/health/ready`.

## 5. Проверка и развёртывание

- [ ] 5.1 Запустить целевые тесты `feature-processing`, `data` и `app`, затем `cd backend-kt && ./gradlew build`.
- [ ] 5.2 Развернуть схему и приложение с `documentation.cleanup.enabled=false`; сверить число заданий миграции с числом заменённых публикаций.
- [ ] 5.3 На тестовой публикации проверить точные ключи, права `DeleteObject`, метрики и отсутствие изменений у другой версии или платформы.
- [ ] 5.4 Включить обработчик конфигурацией и наблюдать размер очереди, возраст старейшего задания, повторы и блокировки.
- [ ] 5.5 Зафиксировать порядок отката: сначала выключить обработчик; аддитивную схему оставить; считать уже удалённые заменённые снимки невосстановимыми.
