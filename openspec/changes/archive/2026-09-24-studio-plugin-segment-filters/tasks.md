## 1. Segment вместо Tabs

- [x] 1.1 В `ui/MainScreen.kt` перевести `ModeSwitch` на `SegmentHorizontal` + `SegmentItem` (`stretch = false`), убрать `Tabs`/`TabItem` и обнуление `contentPadding`/`padding`
- [x] 1.2 В `ui/TokenList.kt` перевести `TokenTypeTabs` на `SegmentHorizontal` + `SegmentItem` внутри `Modifier.horizontalScroll(rememberScrollState())`, убрать `Tabs`/`TabsClip`/`TabItem` и лишние импорты
- [x] 1.3 Задать оба сегмента одним стилем (`Segment.S.Primary`), сверить высоту с `TextFieldM`; при несовпадении подобрать другой размер стиля, а не отступы
- [x] 1.4 Прокручивать выбранный пункт сегмента типов в видимую область (`animateScrollTo`) при смене выбора и при первом показе

## 2. Вертикальный ритм экрана токенов

- [x] 2.1 Добавить в `TokenList` слот режима (`modeSwitch`) и собрать режим, сегмент типов и поиск в одну `Column` с `padding(horizontal = spacing4x, vertical = spacing2x)` и `Arrangement.spacedBy(spacing2x)`
- [x] 2.2 Убрать внутренние `padding(vertical = 2x)` у `ModeSwitch`, сегмента типов и `TokenSearchField`; в `TokensStep` передать `ModeSwitch` в слот
- [x] 2.3 Проверить, что при единственном типе токенов (сегмент типов скрыт) промежуток между режимом и поиском остаётся ровным

## 3. Проверка

- [x] 3.1 Запустить плагин (`./gradlew :plugins:android-studio:runIde` или принятый способ) и вручную проверить hover, нажатие, выбор, светлую и тёмную тему IDE
- [x] 3.2 Проверить узкий tool window: сегмент типов прокручивается, выбранный пункт виден, ритм отступов ровный; решить по индикации скролла (fade — отдельной задачей, если нужно)
- [x] 3.3 Убедиться, что `TokenListTest` проходит без правок (тестируются чистые функции), при необходимости поправить
- [x] 3.4 `cd frontend-kt && ./gradlew :plugins:android-studio:build` (detekt, spotlessCheck, tests); при падении spotless только на изменённых файлах — `spotlessApply` и повторить

## 4. Режим темы только для цветов и градиентов

- [x] 4.1 Добавить `enabled` в `FilterSegment` и `TokenType?.isThemeDependent()` (`color`/`gradient`) с юнит-тестом в `TokenListTest`
- [x] 4.2 Передавать в слот `modeSwitch` признак доступности по выбранному типу и блокировать сегмент режима на остальных типах
- [x] 4.3 Вручную проверить: на «Типографика»/«Тени»/«Формы»/«Размеры» иконка режима приглушена и не кликается, поле поиска не сдвигается, при возврате на «Цвета» режим снова работает

## 5. Режим темы иконкой в строке поиска

- [x] 5.1 Заменить сегмент режима на `IconButton` (`IconButtonSSecondary`) с иконками `SunOutline24` (светлый) / `SleepOutline24` (тёмный), клик переключает режим
- [x] 5.2 Вынести кнопку в слот `modeToggle` `TokenList` и разместить в одной строке с полем поиска справа; убрать `ModeFilter` и параметр `enabled` у `FilterSegment`
- [x] 5.3 Вручную проверить: иконка совпадает по высоте с полем поиска, читается как переключатель темы, доступна на «Цвета»/«Градиенты»
- [x] 5.4 Добавить тултип по наведению на иконку режима (в том числе пояснение на заблокированной)
- [x] 5.5 Вручную проверить тултип: появляется по наведению, не уходит за край панели, не отбирает фокус у поля поиска
