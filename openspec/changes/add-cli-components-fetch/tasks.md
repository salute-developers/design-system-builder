## 1. Предусловие

- [x] 1.0 Перенести backend `components push` на объединённую линию. Сделано rebase
  `feat/components-import` с `dev` на `chore/unify-repositories`: 14 коммитов, все файлы
  переехали в `js/services/db-service`. `feat/components-fetch` переведена на результат,
  `origin/feat/components-import` несёт тот же результат.
  Бэкапы: `backup/import-before-rebase`, `backup/fetch-before-rebase`.
- [x] 1.1 Предусловие снято: `refactor-component-state-sets` завершён и заархивирован (`openspec/changes/archive/2026-08-21-refactor-component-state-sets`, коммит `c04b506`). `drizzle/0004_component_import.sql` в дереве — его редакция, все 8 триггеров модели состояний на месте. Схема этого change лежит поверх, в отдельных миграциях `0005`–`0008`, и `0004` не трогает.
- [ ] 1.2 **Не устранён, нужно решение.** Аудит живой базы: `Checkbox | CheckBox` и `Radiobox | RadioBox` — обе пары присутствуют. Это не разное написание одного компонента: у строк разные словари свойств. `CheckBox` (api-meta) несёт 24 свойства, 2 состояния, 0 appearance; `Checkbox` (прежний сид) — 26 свойств другого поколения (`toggleCheckedBackgroundColor`, `horizontalPadding`, `margin`), 4 appearance и 3 оси, то есть все конфигурации. Слить по имени нельзя — получится химера. `findComponent` (`componentImport.ts:506-512`) выбирает строку без `ORDER BY` и `LIMIT`, поэтому какая из двух получит конфигурации, не определено.
- [x] 1.3 Привести prod-сид к написанию `uikit-api-meta.json`: `js/services/db-service/src/db/seeds/prod/components/checkbox.ts` — `Checkbox` -> `CheckBox`, `js/services/db-service/src/db/seeds/prod/components/radiobox.ts` — `Radiobox` -> `RadioBox`. Два имени из 21.
- [x] 1.4 Обратимость имён проверена на живой базе по правилам `camelToKebab` (`js/apps/client/src/utils/text.ts:1`) и `techToCamelCase` (`plugin_theme_builder/.../StringUtils.kt:23`): 88 имён из 88 обратимы, расхождений нет.
- [x] 1.5 Проверено на живой базе: `properties.type` не содержит ни `gradient`, ни `blur`. Присутствуют ровно 11 типов словаря слотов (dimension 695, color 434, typography 122, component_style 99, shape 90, value 48, float 41, icon 21, shadow 16, boolean 8, integer 5) — сужение перечисления пройдёт без потерь. Прежняя формулировка: Спека объявляет paint-слот как `color`, а перечисление их допускает; найденные привести к типу из api-meta до реализации export, иначе вывод типа значения обопрётся на слот, которого код не порождает.

## 2. Схема db-service

- [x] 2.1 Добавить в `js/services/db-service/src/db/schema.ts` таблицу `appearance_variations`: `appearance_id`, `variation_id`, `position`, `default_style_id`, уникальные индексы по (`appearance_id`, `variation_id`) и (`appearance_id`, `position`).
- [x] 2.2 Добавить таблицу `appearance_variation_values`: `appearance_variation_id`, `style_id`, `position`, уникальные индексы по (`appearance_variation_id`, `style_id`) и (`appearance_variation_id`, `position`).
- [x] 2.3 Добавить колонки `alpha` и `adjustment` типа `text` на `variation_property_values`, `invariant_property_values` и `style_combinations`.
- [x] 2.4 Добавить `token_id uuid REFERENCES tokens(id) ON DELETE set null` на `style_combinations`: единственная из трёх таблиц значений без ссылки на токен, имя лежит в `value` текстом.
- [x] 2.5 Сузить `propertyTypeEnum` до словаря слотов API: `color`, `typography`, `shape`, `shadow`, `dimension`, `float`, `integer`, `boolean`, `icon`, `component_style`, `value`. Снять `gradient` и `blur`. Замер: 40 файлов api-meta, ~52 тыс. параметров — `gradient` и `blur` не встречаются ни разу; `blur` не встречается и в 670 конфигурациях, он попал в перечисление из иллюстративного enum `common_config_scheme.json`. Перечисление совпадёт с `ApiType` и `ParameterType` из `plasma-android`.
- [x] 2.6 Снять `styles.is_default` и партиальный индекс `styles_variation_id_ds_id_is_default_unique` в `schema.ts`. Потребители колонки правятся отдельно, см. 2.7.
- [x] 2.7 Перевести потребителей `styles.is_default` на `appearance_variations.default_style_id`. Мест оказалось больше, чем пять: помимо `catalog.ts`, сидов, генератора и `nl-query`, колонку читали и писали `routes/api/legacy.ts` (чтение `defaultVariations` и два пути создания ДС из legacy-JSON) и `routes/api/component-config.ts` (сборка `defaults`). Все переведены; `db-service` компилируется. Prod- и dev-сиды теперь заполняют и `appearance_variations`, и `appearance_variation_values` — общий помощник `src/db/seeds/appearance_variations.ts`, порядок берётся из порядка объявления, а не из имён. Исходный список: `src/queries/catalog.ts:925` (выборка дефолтного стиля appearance) и `:1251` (поле в проекции), `src/db/seeds/prod/styles.ts` — 187 вхождений, из них 48 со значением `true`, `src/db/seeds/dev/` — 32 вхождения, генератор `src/db/seed-generate-prod.ts:546`, который их печатает, и описание схемы для NL-запросов `src/nl-query/schema-context.ts:68`. Сиды заводят appearance (`seeds/prod/appearances.ts`), поэтому дефолт им есть куда переложить.
- [x] 2.8 Схема этого change лежит **одной** миграцией `drizzle/0005_appearance_declarations.sql` (189 строк): новые таблицы объявлений осей и сочетаний, сужение `property_type`, снятие `styles.is_default` вместе с индексом, колонки `alpha`/`adjustment`/`position` на трёх таблицах значений, `token_id` у `style_combinations`, `native_id`/`native_parent` у объявлений — и следом четыре переноса данных. Изначально их было четыре (`0005`–`0008`), собраны в одну по ходу работы: промежуточные состояния никуда не выкатывались, и дробить их смысла не было. `0004` не тронута, в диффе только `_journal.json`, новый файл и новый снимок. Эквивалентность объединения доказана прогоном: две чистые базы, одна по прежней цепочке из четырёх миграций, вторая по объединённой, `pg_dump --schema-only` совпадает построчно. Единственное расхождение — физический порядок `native_id`/`native_parent` в двух таблицах: `ALTER ADD COLUMN` дописывал их после отметок времени, а `CREATE TABLE` ставит туда, где они объявлены в `schema.ts`, то есть объединённая миграция сходится со снимком точнее прежней цепочки. `drizzle-kit generate` расхождений схемы со снимком не видит.
- [x] 2.9 Перенос данных дописан **перед** `DROP COLUMN is_default` — drizzle ставит удаление последним, и без перестановки читать флаг было бы уже нечем. Заводятся и объявления осей: ось признаётся принадлежащей appearance, только если её стиль участвует в значениях этого appearance напрямую или как участник кросс-осевого сочетания, иначе появились бы фантомные оси. В комментарии миграции записано, что порядок осей и значений невосстановим. Про дефолты формулировка уточнена: перенос **точен** для дизайн-систем, которые вёл клиент — уникальный индекс по (ДС, ось) не позволял завести два разных дефолта, поэтому размножать нечего. Неверен он только там, где гоняли `components push`: прежний `applyDefaults` оставлял в базе последнего записавшего. Замер по пакету: спорят 7 осей из 133, неверных дефолтов 11 из 260 (4.2%). Push на момент change'а не выпущен (`0004` нет ни в `dev`, ни в `master`), значит на проде таких баз нет, а локальные и приёмочную достаточно перезалить.
- [x] 2.10 **Сужения перечисления больше нет — оно не понадобилось.** Прежде drizzle порождал `ALTER COLUMN type SET DATA TYPE text` -> `DROP TYPE` -> `CREATE TYPE` из 11 значений -> `ALTER COLUMN ... USING`, то есть `0005` отыгрывала назад то, что делала `0004`: та заводила `gradient` и `blur` через `ADD VALUE`. Раз `0004` никуда не выпущена (её нет ни в `dev`, ни в `master`, ни на `origin` ни в одной из них), эти два значения убраны прямо из `0004`, а блок сужения снят из `0005`. Минус шесть инструкций, и главное — исчез `DROP TYPE` с перекастом колонки на всей таблице `properties`, единственная по-настоящему рискованная операция change'а. Порядок значений при этом не изменился: `0004` добавляет их ровно в том порядке, который задавало пересоздание. Эквивалентность доказана прогоном двух цепочек на чистых базах — `pg_dump --schema-only` совпадает **побайтово**, 1929 строк, перечисление в обеих: `color, typography, shape, shadow, dimension, float, component_style, value, icon, boolean, integer`. `drizzle-kit generate` расхождений схемы со снимками не видит. Хеш `0004` из-за правки изменился, поэтому в `ds_registry` и `ds_verify` строка миграции переподписана, `db_service_test` пересоздана; `db:migrate` после этого проходит на всех трёх, тесты 46 из 46.
- [x] 2.11 Заполнение `style_combinations.token_id` дописано с фильтром по `properties.type IN ('color','typography','shape','shadow')`. Прогон на копии живой базы: 342 объявления осей (261 с дефолтом), 1322 значения осей, 103 ссылки на токен из 1573 строк. Низкая доля — свойство этой базы, а не запроса: она смешивает ДС `base` с 1335 токенами и dev-ДС `SDDS`/`PLASMA` с 16, чьи сочетания ссылаются на чужие токены. Доли совпадают с уже существующими `variation_property_values` (color 38% против 44%, shape 49% против 40%), то есть backfill ведёт себя как импорт.
- [x] 2.12 `js/docs/db-schema.dbml` обновлён вручную: две новые таблицы, `alpha`/`adjustment` на трёх таблицах значений, `token_id` у `style_combinations`, снятие `styles.is_default` вместе с партиальным индексом. Заодно исправлено расхождение, накопленное до этого change: `Enum property_type` содержал 6 значений вместо 11 — не было `component_style`, `value`, `icon`, `boolean`, `integer`. Сверено со `schema.ts` программно: состав таблиц совпадает в обе стороны, перечисление совпадает поэлементно.

## 3. Импорт

- [x] 3.0 Тестовый харнесс `db-service` поднят: `vitest` в devDependencies, скрипты `test` и `test:watch`, `vitest.config.ts`. Тесты идут против настоящего Postgres, а не мока — логика импорта почти целиком SQL, и мок проверял бы мок. Адрес берётся из отдельного `TEST_DATABASE_URL` (по умолчанию база `db_service_test` на том же 5433), чтобы тесты нельзя было по невнимательности навести на рабочую базу. `global-setup` создаёт базу, если её нет, и накатывает `drizzle/` — тесты видят ту же схему, что рабочий контур, вместе с триггерами. Каждый тест выполняется в транзакции и откатывается (`src/test/database.ts`, `withRollback`), поэтому базу не надо чистить руками и порядок тестов не влияет на результат. `tsconfig` исключает тесты из сборки — проверено, в `dist` они не попадают. Конвенция записана в `js/CLAUDE.md`.
- [x] 3.1 `alpha` и `adjustment` пишутся во все три таблицы значений. Сериализация вынесена в общий `rawToText` (`commonConfig.ts`): форма сохраняется как есть, `0.2` не превращается в `0.20`. `ExpandedValue` несёт оба поля; у переопределения состояния своя `alpha`, а `adjustment` у него нет — общий формат объявляет его только на самом значении.
- [x] 3.2 `expandStates` берёт `state.type ?? property.type`. Проверено измерением на живой базе: конфигурация, где базовое значение имеет тип `value` (токенов не несёт), а переопределение `pressed` объявляет `color`, до правки давала у состояния `token_id = NULL`, после — ссылку на токен. `alpha` состояния пишется в его же строку: замер дал `0.2` у базы и `0.5` у переопределения, текстом и без нормализации.
- [x] 3.3 `token_id` резолвится и пишется для строк `style_combinations`, `set` у `onConflictDoUpdate` обновляет его вместе с `value`, `alpha` и `adjustment` — иначе повторный импорт оставил бы ссылку от прежнего значения. Попутно снята причина расхождения: у `writeCombination` была своя копия развёртки состояний, из-за которой кросс-осевые значения теряли и `alpha`, и ссылку на токен. Теперь она общая — `expandStates`.
- [x] 3.4 Заводить строки `appearance_variations` на каждую ось `config.variations`, `position` — индекс в порядке следования осей.
- [x] 3.5 Заводить `appearance_variation_values` на каждое значение оси, включая значения, встречающиеся только в `targets`.
- [x] 3.6 `applyDefaults` заменён на `declareAxes`: состав, порядок, дефолт и удаление снятых объявлений одним проходом. Дефолт ставится этому appearance и не трогает другие.
- [x] 3.7 Удалять объявления осей и значений, которых больше нет в конфигурации, тем же порядком, что и `clearAppearanceValues`.
- [x] 3.8 `validate()` перестал выводить допустимые типы значения из `propertyTypeEnum` и получил собственное множество `VALUE_TYPES`: словарь слотов плюс `gradient`, без `blur`. `integer` оставлен, хотя в 670 конфигурациях не встречается ни разу: отклонять значение за тип, который слот законно носит, незачем. Заодно тем же словарём проверяется тип переопределения состояния — прежде он не проверялся вовсе. **Это было не наведение порядка, а починка:** после сужения перечисления (2.5) `validate()` отклонял бы любую конфигурацию с `gradient`, то есть все 54 градиентных значения корпуса. Измерено — с выводом из перечисления два теста падают с `Unsupported property type 'gradient'`.
- [x] 3.9 `PAINT_TYPES = {color, gradient}` добавлено в `sameTypeFamily` третьей ветвью. Проверено измерением: без неё тест на градиент в цветовом слоте падает с `backgroundColor: global 'color', config 'gradient'`. Заодно исправлен `collectPropertyTypes` — он собирал только `property.type` и не видел типы переопределений состояний, хотя они тоже объявлены конфигурацией: у `slider.thumbStrokeColor` база цветовая, а `pressed` градиентное. Четыре свойства с единственным типом `gradient` (`avatar/background`, `avatar/textColor`, `rect-skeleton/gradient`, `text-skeleton/gradient`) в `typeMismatches` больше не попадают.
- [x] 3.10 Отчёт `gradientOnlyProperties` заведён: накопитель `paintTypesByProperty` в `ImportContext` копит виды заливки по паре `компонент.свойство` за весь пакет, итог подводится в конце импорта. Заполняется в `findProperties`, где известны и слот, и объявленные конфигурацией типы. Заодно дополнена схема ответа в `openapi/spec.ts`: там не было ни `unknownStates`, ни `typeMismatches` — оба поля отчёт возвращал, а контракт о них молчал.
- [x] 3.11 Аудит по всему сервису: `insert`/`update`/`delete` над `properties` есть только в сидах, в REST-ручке `routes/api/properties.ts` и в фикстуре тестов. В `db/import/` — ни одного, то есть импорт слот не пишет ни на одном пути. Наблюдение закреплено двумя тестами: конфигурация, объявляющая расходящиеся типы в обе стороны семей и ссылающаяся на несуществующее свойство, не меняет ни одной строки `properties` — сверяется весь снимок, включая `updated_at`, поэтому холостой `update` тоже был бы виден; и повторная заливка второй ДС с градиентом оставляет слот `color`. Проверено измерением: имитация записи слота импортом роняет 7 тестов из 16.
- [x] 3.12 Гранулярность закреплена тестом. Первая редакция теста её **не** проверяла: два стиля шли в порядке «градиентный, потом цветной», и накопитель по конфигурации тоже дал бы пустой отчёт, потому что последним оказывался цветной. Порядок переставлен на «цветной, потом градиентный» — теперь подмена пакетной гранулярности на поконфигурационную роняет тест с `expected [ 'TestButton.background' ] to deeply equal []`. Проверено обеими прогонками.
- [x] 3.13 Тестов импорта стало 20, все проходят. Покрыты: `alpha`/`adjustment` в исходной текстовой форме; состояние со своим типом и своим токеном; вид заливки по токену при неизменном слоте; `gradient` принимается, `blur` отклоняется, тип состояния проверяется тем же словарём; paint-семья и настоящее расхождение семей; отчёт `gradientOnlyProperties` и его пакетная гранулярность; неприкосновенность глобального слоя; свойство с разными видами заливки в разных значениях оси; объявленное значение оси без свойств; разные дефолты одной оси у двух appearance одного компонента; кросс-осевое значение с разрешённым токеном и `alpha`. Каждый новый тест проверен на различающую силу: имитация «объявленные значения только по использованным», «дефолт не на уровне appearance» и «сочетания без token_id» роняет ровно по одному тесту.

## 4. Export endpoint

- [x] 4.1 `src/routes/api/component-config-export.ts` создан и подключён к `/ds/component-config` рядом с import-router. Тело валидируется `ExportRequestSchema` (`designSystemId`, uuid), дизайн-система резолвится с тем же project-фильтром, что у `/import`.
- [x] 4.2 Проверка scope вынесена в общий `requireScope(scope)` в `routes/api/utils.ts` вместо копии: import-router переведён на неё, export использует её же с `components:read`. Проверка стоит до разбора тела.
- [x] 4.3 `meta.name` из `design_systems.name`, `meta.version` из последней опубликованной версии по `published_at`. Отказ `422` при отсутствии версий проверен живьём: сид без версии даёт `Design system 'base' has no published version`.
- [x] 4.4 `componentName` выводится `camelToKebab` с проверкой обратимости через `techToCamelCase`; при несходимости — отказ `422` с именем компонента и обоими вариантами написания.
- [x] 4.5 Оси берутся из `appearance_variations` в порядке `position`, значения — из `appearance_variation_values` в порядке `position`. Из наличия строк значений ничего не выводится.
- [x] 4.6 Common-формат отдаётся именами осей: `rootVariationId`, `colorSchemeVariationId`, `variations[].id`, `defaults[].id`, `targets[].properties[].id`.
- [x] 4.7 Примитивы значений boolean-осей реализованы (`axisValue`): ось, объявленные значения которой суть `true` и `false`, отдаётся с JSON-булями. В prod-сиде boolean-осей нет, поэтому проверка — за тестами 4.13.
- [x] 4.8 Тип значения выводится по каждой строке отдельно и применяется к четырём формам. Размещение: `default` для `color` и `gradient`, `value` для остальных. Добавлено обратное преобразование JSON-типа (`restoreJsonType`): в базе всё лежит текстом, и без него `float` возвращался строкой `"0.4"` вместо `0.4`.
- [x] 4.9 `states[].type` пишется только при расхождении с выведенным типом базового значения.
- [x] 4.10 `underivedTypes` собирается для paint-значений без разрешённого токена. На prod-сиде даёт 12 записей — `inherit` и `transparent` в цветовых слотах: в paint-слоте литералов не бывает, `tokenNameOf` трактует любое значение как имя токена, поэтому такие значения и правда невыводимы.
- [x] 4.11 **Снято по решению: `unknownProperties` убрано из контракта выгрузки.** Поле оказалось невыводимым: импорт эти значения не записывает и нигде не сохраняет, строк с такими свойствами нет по построению — `property_id` защищён внешним ключом. Факт принадлежит загрузке, и `push` уже возвращает его в своём отчёте. Убрано из `componentExport.ts`, `backend-contract.md`, спеки `component-appearance-model` и требования fetch в `cli-components`; в `design.md` записано, почему сообщает push, а не fetch.
- [x] 4.12 Порядок ответа детерминирован: компоненты по `componentName`, затем `styleName`; свойства по имени; состояния по набору.
- [x] 4.13 Тестов стало 37 в трёх файлах. Выгрузка проверяется на состоянии, построенном **импортом**, а не вставками руками: так тест не может разойтись с тем, что кладёт в базу push, и заодно проверяется сам круг. Покрыты: отказ без опубликованной версии; выбор последней версии по времени; отказ на необратимом имени; градиент у инварианта и у переопределения состояния; отсутствие `states[].type` при совпадении с базой; `underivedTypes` при неразрешённом токене; литерал вне paint в отчёт не попадает; возврат JSON-типа числам; объявленное значение оси без свойств; примитивы boolean-оси; градиент у значения оси и у кросс-осевого сочетания. Scope проверяется отдельным файлом `routes/api/utils.test.ts` — шесть случаев, включая похожий префикс `components:read-only` и системного администратора. Три имитации регрессий (без примитивов boolean, тип состояния всегда, оси по uuid) роняют ровно по одному тесту.
- [x] 4.14 Фикстуры вынесены в `src/test/fixtures.ts`: их делят тесты импорта и выгрузки.
- [x] 4.15 Комментарий в `component-config.ts` переписан. Прежний утверждал, что `type` допускает больше вариантов, чем `propertyTypeEnum`, и перечислял `gradient`, `component_style`, `value`, `blur` — из них в перечислении были все четыре, то есть утверждение было неверно и до сужения. Теперь сказано по существу: `type` — тип значения, а не слота, и словарь у него свой — шире перечисления на `gradient` и уже на `integer`, со ссылкой на `VALUE_TYPES`.
- [x] 4.16 `/sync-all` прогнан целиком. `validation/schema.ts`: снят `isDefault` у стилей, добавлены схемы двух новых таблиц, `alpha`/`adjustment` на трёх таблицах значений и `token_id` у сочетаний. `routes/api/`: заведены `appearance-variations.ts` и `appearance-variation-values.ts`, зарегистрированы под `/ds/`. `openapi/spec.ts`: обе таблицы плюс описание самой ручки `POST /ds/component-config/export`, которой в спеке не было. `apps/admin/src/api/{openapi.json,types.gen.ts}` перегенерированы.
- [x] 4.17 Сужение перечисления дошло до админки, и её компилятор это поймал — ровно как обещал комментарий в `SettingsPage.tsx`. Из захардкоженного `PROP_TYPES` убраны `gradient` и `blur`, в комментарии записано, что это словарь типов слота, а не значения.

## 5. Существующая ручка `GET /ds/component-config`

- [x] 5.1 Состав потребителей зафиксирован: у `GET /ds/component-config` их нет ни одного. Проверено во всех трёх репозиториях — в этом обращений нет ни из `apps/client`, ни из `apps/admin`, ни из `services/{generator,documentation-generator,publisher}`, ни из `js/cli`, ни из `frontend-kt/cli` (CLI ходит только в `/import`); в `theme-converter` вызовов нет; в `plasma-android` плагин берёт тему из локального `.sdds`, а не по HTTP — спека `theme-builder-dsbuilder-source` описывает именно чтение файлов. Ручка заведена коммитом `81221e9 feat(db-service): Add endpoint for common config generate` и потребителя так и не получила. Legacy-маршрут `component-configs`, который мы не трогаем, потребителей два: `apps/client` и `documentation-generator` (`documentation.service.ts:222`).
- [x] 5.2 Ручка отдаёт выведенный тип значения вместо слота. Правило вынесено в общий модуль `src/db/export/valueType.ts` и используется обеими сторонами: вторая копия означала бы, что одно и то же хранимое значение возвращается с разным типом в зависимости от того, кто спросил. Ручка стала грузить `tokens.type` рядом с именем — прежде брала только имя. Проверено по HTTP на засеянной базе.
- [x] 5.3 Значение размещается по типу: `default` у цвета и градиента, `value` у остальных. Прежде всё безусловно клалось в `value`. Заодно добавлен возврат JSON-типа: `float` уходил строкой `"0.4"`, теперь числом — на живой проверке `loadingAlpha` вернулось `0`, а не `"0"`.
- [x] 5.4 `states[].type` отдаётся только при расхождении с типом базового значения — тем же правилом, что у export. Проверено живьём: цветная база с градиентным переопределением `pressed` возвращает `type: "color"` у базы и `type: "gradient"` у состояния.
- [x] 5.5 Оси берутся из `appearance_variations` в порядке `position`, значения — из `appearance_variation_values`. Прежний вывод «есть ли стиль этой оси в дизайн-системе» давал фантомные оси: стиль заводит любой appearance компонента, и ось попадала в ответ соседям с пустым набором значений. Порядок осей тоже больше не случайный — он из объявления, а не из порядка строк `variations`.
- [x] 5.6 Тесты ручки добавлены (`routes/api/component-config.test.ts`): вид заливки по токену значения, размещение в `default`, `states[].type` только при расхождении, возврат JSON-типа числам, отсутствие фантомной оси у соседнего appearance, сохранение объявленного значения без свойств. Ручка ходит в базу через синглтон `db`, поэтому откатом её не изолировать — состояние заводится и убирается явно, а `vitest.config.ts` подменяет `DATABASE_URL` на тестовый адрес, чтобы прогон физически не мог попасть в рабочую базу. Две имитации прежнего поведения (оси из наличия стилей, значение всегда в `value`) роняют тесты.

## 6. Codec

- [x] 6.1 `NativeAccumulator` пишет `variation.name` в `bindings[].name` и `binding[].name`, сопоставление ведёт по `id`. Без этого выгруженная конфигурация получила бы uuid вместо `size` везде, где два поля различаются.
- [x] 6.2 `decode` переносит объявленные значения оси из `bindings[].values` в common. Порядок объявления восстанавливается для собственных значений оси, и неиспользованное встаёт на своё место, а не в конец: `plasma_homeds/avatar` объявляет `shape: [default, pilled]`, а переопределения несёт только `pilled`. Записи с `targets` не переставляются — одно имя встречается в них по разу на каждое пересечение.
- [x] 6.3 `encode`: значение без свойств и без пересечений остаётся в `bindings[].values` и вариации не порождает. Пустая вариация в native означала бы другое — сочетание без единого свойства.
- [x] 6.4 Вид оси выводится: `view` по роли цветовой схемы, `boolean` если все значения суть `true`/`false`, иначе `enum`. Требовать оба значения нельзя — в корпусе есть оси, где используется только `true`.
- [x] 6.5 Размещение значения по типу отдельного кода не потребовало: `CommonConfig` переиспользует `NativeProperty`, поэтому `default` и `value` проходят насквозь в обе стороны. Закреплено сверкой инвариантов и свойств сочетаний в round trip по корпусу.
- [x] 6.6 `alpha` и `adjustment` проходят обе стороны без нормализации по той же причине — общая модель значения. Закреплено той же сверкой.
- [x] 6.7 Round trip по корпусу ужесточён тремя сверками: объявленные значения осей и их порядок, виды осей, JSON-тип значений boolean-осей. Сверка ведётся на сохранность, а не на совпадение списков: у части конфигураций корпуса объявление беднее фактического использования, и восстановленная конфигурация его добирает — это пополнение, а не потеря. Из сверки видов исключена ось цветовой схемы: общий формат несёт её роль, а не объявленный вид, и `enum` для оси, исполняющей эту роль, восстановить неоткуда (`sdds_sbcom/checkbox`).
- [x] 6.8 Добавлены три теста `encode`: конфигурация, где `id` оси отличается от `name` (проверяет, что в native уходит имя, а не uuid); объявленное значение без переопределений остаётся в `bindings[].values` и не порождает вариации; булева ось получает тип `boolean` и значения-примитивы.

## 7. CLI: domain

- [x] 7.1 `ExportedComponentPackage` — имя, версия, конфигурации в common-формате (`ExportedComponentConfig` с парой `componentName`/`styleName`) и `underivedTypes`.
- [x] 7.2 `ExistingComponentPackage` — записи существующего `meta.json` и перечень файлов директории. Отсутствие `meta.json` отказом не является: в пустую директорию пакет пишется целиком.
- [x] 7.3 `ComponentPackageWritePlan` — файлы конфигураций с содержимым, `meta.json` и перечень несвязанных файлов. Плюс `RenderedComponentConfig` для конфигурации, уже преобразованной кодеком.
- [x] 7.4 `ComponentPackageWritePlanBuilder` собран. Правило имени выведено из корпуса: `styleName` с заменой `-` и `.` на `_` плюс `_config.json` — 625 записей из 661 ложатся на него точно, 36 отклонений дают шесть лексических случаев (`check-box` лежит как `checkbox_config.json`, `modal-bottom-sheet` — как `bottom_sheet_config.json`), размноженных по восьми пакетам. Именно поэтому имена из существующего `meta.json` переиспользуются, а правило применяется только к новым парам. Имя строится из стиля, а не из компонента: `componentName` не уникален.
- [x] 7.5 Построитель к файловой системе не обращается: состояние директории получает моделью `ExistingComponentPackage`, возвращает либо план, либо `Failed` с deterministic сообщением.
- [x] 7.6 Семь тестов построителя на доменных моделях: правило имени, переиспользование имён существующего пакета, применение правила только к новым парам, порядок по `componentName` и `styleName` в файлах и в `meta.json`, коллизия имён с перечислением обеих пар, вычисление сирот с исключением `meta.json`, содержимое `meta.json`. Всего в CLI 162 теста, `detekt` и `spotlessCheck` чисты.

## 8. CLI: application

- [x] 8.1 `ComponentConfigRemoteSource` расширен операцией `export` и перестал быть `fun interface`. Реализация и fake в тестах push обновлены: fake стал классом, у которого `export` падает с сообщением «push не выгружает пакет» — обращение к ней означало бы ошибку в барьерах.
- [x] 8.2 Объявлен port `ComponentPackageDirectoryReader` с `ComponentDirectoryReadResult`. Отдельно от `ComponentPackageLoader`: тот читает пакет целиком и отказывает при отсутствии файла из `meta.json`, а выгрузке нужен противоположный контракт — прочитать имена из `meta.json`, каким бы неполным он ни был.
- [x] 8.3 Объявлен port `LocalComponentPackageWriter` с result-типом и deterministic сообщением отказа.
- [x] 8.4 `FetchComponentsUseCase` реализован: разрешение API URL, project context и credentials, запрос через port, преобразование кодеком, чтение директории, построение плана в `domain`, запись через port.
- [x] 8.5 Отказ всего fetch на первой ошибке преобразования, с указанием компонента и стиля, **до** чтения директории и записи. Порядок закреплён тестом: фейки отмечают, что их не вызвали. Метод разделён на разрешение и запись не ради длины — до этой границы ни один файл не тронут, после неё трогается рабочая копия.
- [x] 8.6 Use case не содержит пути запроса, конфигурации `Json`, вызовов сериализации и обращений к `CliFileSystem`: путь и wire-модели живут в `data`, сериализация плана — в построителе.
- [x] 8.7 Шесть тестов на фейках портов: один запрос с верной адресацией и запись; отказ преобразования до файловой системы; отказ backend; отказ построителя плана; отсутствие credentials до запроса; перенос `underivedTypes` в результат. Всего в CLI 168 тестов, `detekt` и `spotlessCheck` чисты.

## 9. CLI: data

- [x] 9.1 `@Serializable` модели `ExportRequest` и `ExportPackageResponse` объявлены в `data`: `application` получает доменную `ExportedComponentPackage` и о форме JSON не знает.
- [x] 9.2 `gradientOnlyProperties` добавлено в wire-модель отчёта импорта и в доменную `ComponentImportReport`, а `components push` печатает его отдельной секцией «Paint properties without a solid colour» — не среди расхождений типа: слот `color` покрывает и градиент, поэтому это расхождение оформления и кода, а не ошибка типа.
- [x] 9.3 `LocalComponentPackageDirectoryReader` реализован поверх `CliFileSystem`. Отсутствие директории и отсутствие `meta.json` отказом не являются — в пустую директорию пакет пишется целиком, — а вот нечитаемый `meta.json` отказ: пустое состояние вместо него означало бы, что выгрузка молча переименует все файлы пакета, потеряв уже выбранные имена.
- [x] 9.4 `LocalComponentPackageFileWriter` реализован: создаёт директорию при отсутствии и пишет файлы плана. Решений о составе и именах не принимает. `meta.json` пишется последним: он описывает состав директории, и до записи всех конфигураций такое описание было бы неверным.
- [x] 9.5 Четыре теста реализации порта: один POST по пути `/api/projects/{id}/ds/component-config/export`, адресация телом по `designSystemId` с проверкой, что лишних полей в теле нет; разбор ответа в доменную модель вместе с `underivedTypes`; отказ при нечитаемом теле успешного ответа; проброс ошибки транспорта.
- [x] 9.6 Шесть тестов reader и writer на `InMemoryCliFileSystem`: пустое состояние при отсутствии директории, чтение записей и перечня файлов с отсевом посторонних, отказ на нечитаемом `meta.json`, явная директория из `--to`, запись плана с созданием директории, сохранность несвязанных файлов. Диск для этого не нужен — реализации работают с текстом и перечнем директории.
- [x] 9.7 Тесты reader и writer на `InMemoryCliFileSystem` добавлены (см. 9.6).

## 10. CLI: presentation и DI

- [x] 10.1 `ComponentsFetchCliCommand` добавлена с опциями `--to`, `--api-key`, `--api-url`.
- [x] 10.2 Команда зарегистрирована в `ComponentsCliCommand` и `ComponentsFeatureModule` вместе с новыми портами, построителем плана и use case.
- [x] 10.3 Вывод: имя и версия пакета, число конфигураций, API URL с источником, `projectId`, `designSystemId`, целевая директория и число записанных файлов. API key не печатается ни в каком виде.
- [x] 10.4 Списки значений с невыведенным типом и файлов, оставшихся от прежнего состава, печатаются отдельными секциями. Ни один не отменяет запись и не даёт ненулевой статус: пакет уже записан, а оба списка означают расхождение между тем, что лежит в директории, и тем, что из неё соберётся.
- [x] 10.5 Presentation печатает доменные модели: из результата убран сам план записи — вместо него `Fetched` несёт имена записанных файлов и перечень несвязанных. Проверено грепом: команда импортирует только `application`, ни `data`, ни `ComponentPackageWritePlan`.
- [x] 10.6 Тесты дерева команд: `components --help` перечисляет `fetch`, `components fetch --help` показывает все три опции и не требует ни project config, ни backend, ни credentials. Всего в CLI 180 тестов; `./gradlew :cli:build` проходит целиком.
- [x] 10.7 Тесты `ComponentsCliCommandTest` добавлены (см. 10.6).

## 11. Верификация

- [x] 11.1 `cd js && npm run build` проходит: db-service, generator, documentation-generator, client и admin — все пять целей с кодом 0. Зависимости пришлось доставить в `services/generator`, `services/documentation-generator` и `apps/client` — их не было в дереве.
- [x] 11.2 Миграции на чистой базе `ds_verify` применились полностью: 6 записей в журнале, обе новые таблицы созданы, три колонки (`token_id`, `alpha`, `adjustment`) на `style_combinations` есть. **Восемь триггеров модели состояний из `0004` живы** — `0005` их не тронула.
- [x] 11.3 `npm run db:seed:prod` на чистой базе прошёл: 48 объявлений осей, 186 значений, 186 стилей, 1159 значений вариаций. `components` содержит `CheckBox` и `RadioBox` в написании api-meta, дублей по канону нет.
- [x] 11.4 `npm test` в `db-service`: 42 теста в четырёх файлах, все проходят.
- [x] 11.5 Результат `/sync-all` в диффе целиком: `validation/schema.ts`, `routes/api/*` вместе с двумя новыми файлами маршрутов, `routes/index.ts`, `openapi/spec.ts`, `js/apps/admin/src/api/{openapi.json,types.gen.ts}` и правленный руками `js/docs/db-schema.dbml`. Повторная генерация ничего не меняет — артефакты актуальны, а не отстали.
- [x] 11.6 `cd frontend-kt && ./gradlew build` проходит целиком, с `--rerun-tasks`.
- [x] 11.7 `./gradlew detekt spotlessCheck test` во всём `frontend-kt` проходит с `--rerun-tasks`, без единого замечания.
- [x] 11.8 Round trip кодека прогнан на корпусе после ужесточения сверок: `nativeRoundTripPreservesExpressiblePart`, `commonRoundTripIsExact`, `corpusDeserializesWithoutLoss` и `corpusCoversRequiredEdgeCases` проходят. Всего в CLI 180 тестов, падений нет.
- [x] 11.9 Границы слоёв проверены по импортам: `domain` не импортирует ни `data`, ни Ktor; `application` не импортирует ни Ktor, ни `CliFileSystem`, ни `data`; `presentation` не импортирует ни `data`, ни `@Serializable` модели.

## 12. Локальная приёмка

- [x] 12.1 `sdds_serv` залит в чистую базу через `components push --apply`: **143 конфигурации из 144 создано**. Отклонена одна — `bottom-sheet (modal-bottom-sheet)`, компонента нет в глобальном слое: это ровно случай алиаса из 13.2. Отчёт назвал 14 свойств вне глобального слоя и **ровно те четыре paint-свойства, что предсказал замер по корпусу** — `avatar.background`, `avatar.textColor`, `rect-skeleton.gradient`, `text-skeleton.gradient`. Контур приёмки: чистая база `ds_verify`, глобальный слой из настоящего api-meta (83 компонента, 1540 свойств), 2227 токенов из `themes/sdds_serv/latest.zip` (204 градиентных), `db-service` на 3009, прокси на 3010 вместо gateway, CLI из `cli-jvm.zip`.
- [x] 12.2 `components fetch` в пустую директорию прошёл: 143 файла конфигураций плюс `meta.json`. `underivedTypes` назвал два значения — `switch.toggleThumbColor` и `switch.toggleTrackColor`, чьи токены отсутствуют в загруженном наборе.
- [x] 12.3 `components fetch` выполнен поверх копии исходного пакета `theme-converter` (144 конфигурации, контур `ds_verify`). **Имена файлов не изменились: список из 145 путей совпадает целиком, ни одного нового, ни одного пропавшего.** Отклонённый `bottom_sheet_config.json` fetch не тронул и назвал его сам — «Files left from a previous composition (not removed)». **Дифф сверку не прошёл** и вскрыл три дефекта выгрузки, которых не увидели ни круг push/fetch, ни сборка темы, потому что оба контроля сравнивают выгрузку с выгрузкой, а не с чужим производителем формата:
  - значения булевых осей писались строкой `"true"` вместо `true` в `variations[].binding[].value` (62 места). `asAxisPrimitive` применялся только к `defaultValue`, хотя комментарий утверждал обратное. Исправлено в `ConfigCodec.kt`: вид оси считается один раз и передаётся сборщику вариаций.
  - вложенный `binding` внутри записей `view` не писался вовсе (525 узлов). Замер показал, что все 525 — ровно `{name: <ось схемы>, value: <ключ записи>}`, то есть сведений не терялось и плагин их восстанавливал, но с исходным пакетом расходился каждый файл, где есть `view`. Исправлено в `addSchemeValue`.
  - файлы конфигураций писались без перевода строки в конце, тогда как все 144 исходных им заканчиваются. Исправлено в `ComponentPackageWritePlanBuilder`; `meta.json` по-прежнему без него — там так в исходнике.
  После правок: файлов, совпадающих по смыслу, стало 74 из 144 вместо 57; расхождений по содержимому `binding` — 0 вместо 62; пропавших ключей внутри `view` — 0 вместо 525. Дифф 57 186 строк → 52 091.
  **Остаток объяснён и содержательных потерь сверх принятых не содержит.** 73 файла различаются только оформлением: порядок ключей внутри `props` (у нас алфавитный, в исходнике авторский) и пустые контейнеры `"view": {}` / `"variations": []` / `"parent": null`, которые наш сериализатор опускает (`encodeDefaults = false`, `explicitNulls = false`). После канонизации обеих сторон от 52 091 строки остаётся 26 596 — примерно половина диффа не несёт смысла вовсе. Оставшиеся 70 файлов различаются по смыслу, и каждый класс уже известен: 55 свойств вне глобального слоя (push называет 14 имён), порядок состояний внутри набора (канонизирует триггер `0004`), 321 место с порядком элементов `binding`. Отдельно проверено `duration` в `rect-skeleton`/`text-skeleton`: исходник объявляет `float`, глобальный слой из api-meta — `integer`, и выгрузка отдаёт `integer`. Это верное поведение, а не дефект: `properties.type` — слот API, `float` и `integer` в одной числовой семье `sameTypeFamily`, поэтому расхождение и не попадает в «Property type mismatches».
  Порядок ключей `props` не восстановим без хранения авторского порядка свойств в модели — отдельного столбца под это нет, и заводить его этот change не берётся. Практического веса это не имеет: `fetch` идемпотентен (12.2 — две подряд выгрузки побайтово равны), поэтому дифф поверх *своей же* прежней выгрузки читаем по построению, а 12.3 сравнивает с чужим производителем формата.
- [x] 12.4 **Круг стал неподвижной точкой с первой итерации.** Обратный `push --apply` результатом fetch: 0 создано, **143 без изменений, 0 обновлённых, 0 отклонённых**. Прежние 29 обновлений исчезли — это были ровно те конфигурации, чьи координаты модель не хранила. Две последовательные выгрузки совпадают побайтово по всем 144 файлам, `pagination-dots` сохраняет `m.line` и `s.line`.
- [x] 12.5 Типы значений на круге сверены косвенно и убедительно: раз обратный push даёт 143 «без изменений», ни одно значение не изменило ни вида заливки, ни размещения — иначе сигнатура разошлась бы и строка попала бы в «обновлено». Прямая сверка градиентов остаётся к 12.7, где тема собирается плагином.
- [x] 12.6 Замер сделан на контуре `ds_verify`: тело **647 653 байта** (633 KiB) на 143 конфигурации, время ответа **48–64 мс**, медиана 51 мс на пяти прогонах после прогрева. Отдельно проверено сжатие: с `accept-encoding: gzip` тело приходит тем же размером — compression-middleware в `db-service` нет. Числа и вывод про сжатие записаны в `backend-contract.md`, раздел 4.
- [x] 12.7 Тема собрана плагином `plugin_theme_builder` из выгруженного пакета и сверена с темой из исходного. **Сборка проходит, 609 файлов из 633 совпадают побайтово.** Различаются 22, потеряно 2, ещё 12 появляются только в сборке из выгрузки — остаток разбирается в 13.9. Числа исправлены при разборе 13.2: первый замер (611 совпало, 0 потеряно, 16 лишних) снят с незачищенного каталога вывода — `generateComponents` не удаляет прежние файлы, и в снимок попадали остатки предыдущих сборок. Пересобрано с `rm -rf build/generated` перед каждым прогоном. По дороге сборка нашла три дефекта, которых не видели ни тесты, ни круг push/fetch: JSON-були в `bindings[].values`, потерянный дефолт булевой оси и переставленный порядок переопределений состояний. Фикстура `test_components.zip` в `plasma-android` восстановлена, рабочее дерево того репозитория чисто. **Пересобрано после правок 12.3** (булевы значения осей, вложенный `binding`, перевод строки): сборка из исправленной выгрузки **побайтово совпадает** со сборкой из прежней — 643 файла, те же 609 совпадений, 22 различия, 2 потери, 12 лишних. То есть правки 12.3 меняют форму пакета, но не то, что из него порождает плагин, и вывод 12.7 остаётся в силе. Круг push/fetch на исправленном кодировщике тоже проверен заново: 143 «без изменений», 0 отклонено, две выгрузки подряд побайтово равны.

## 13. Открытое к решению

- [x] 13.10 Отчёт push стал показывать неразрешённые ссылки. Доменная модель `ComponentImportReport` в CLI вовсе не имела полей `unresolvedTokens` и `unresolvedComponentStyles`: backend их возвращал, wire-модель отбрасывала, и пользователь не узнавал о ссылке в пустоту никогда. Добавлены в доменную и wire-модель, печатаются отдельными секциями. На живом прогоне `sdds_serv` всплыли 40 ссылок — список именами, а не строками значений, поэтому читаемый: 23 типографики, 7 форм, 2 цвета и далее.

- [x] 13.8 Идентификатор вариации теперь хранится. Правило вывода корпус отверг: из 4600 сегментов под «сегмент равен значению оси» подходят 3773, но 798 выглядят как `outer-label` у оси `label-placement` со значением `outer`, у булевых осей 187 равны имени оси и ещё 125 — сокращению вроде `shadow` у `has-shadow`, а у 369 вариаций число сегментов вообще не совпадает с числом осей. Идентификатор авторский, и плагин строит из него имя стиля (`VariationNode.camelCaseName` разбивает `id` по точкам и переводит сегменты в CamelCase). Родитель пришлось хранить тоже: у `basic_button` в `sdds_sbcom` есть вариация с двумя осями и `parent: null` — из координаты такого не вывести. Общий формат получил `nativeId` и `nativeParent` на значении оси, схема — колонки на `appearance_variation_values` и `appearance_combinations` (миграция `0005_appearance_declarations`), round trip кодека — сверку идентификаторов и родителей по координате. Результат на собранной теме: **611 файлов из 633 совпадают побайтово** против 562 до правки, различий 22 против 71, потерянных по-прежнему ноль.
- [x] 13.9 Остаток сверки разобран по классам (перемерено на чистых сборках, см. 12.7). Из 22 различающихся файлов **9 отличаются только порядком строк**, ещё **7 — порядком состояний внутри набора** (`setOf(Focused, Checked)` против `setOf(Checked, Focused)`): набор в Kotlin неупорядочен, и в ColorStateList все состояния элемента должны совпасть, поэтому смысла это не меняет. Порядок внутри набора модель и не хранит — `state_sets.state_ids` канонизируется триггером миграции `0004`. Оставшиеся **6 файлов — чистая потеря**, и каждая потерянная строка приводит к свойству из отчёта push «properties absent from the global layer»: `disableAlpha`, `minContentHeight`, `prefixPadding`, `suffixPadding`. Это принятая потеря change'а, работающая ровно как описано, и push о ней сообщает. **2 потерянных файла** — пара `ModalBottomSheet`: её конфигурация отклонена на push (компонента нет в глобальном слое), поэтому в выгрузке её нет вовсе; это тот же случай, что 12.1 и 13.2, а не потеря выгрузки. Все 12 лишних файлов — View-стили `text-field-clear` и `text-area-clear`: Compose-стили для них есть в обеих сборках (314 вхождений против 955), то есть данные не появились из ниоткуда, но условие, по которому плагин порождает XML-стиль, изменилось вслед за потерей четырёх свойств вне глобального слоя. Отдельной задачи это не требует: исчезнет вместе с расхождением оформления и кода. Прежнее утверждение о «4 восстановленных файлах `pagination-dots`» снято — это был артефакт незачищенного каталога: на чистой пересборке координаты `m.line` и `s.line` присутствуют в обеих сборках.

- [x] 13.7 Объявление сочетания заведено по образцу объявления оси: `appearance_combinations` (appearance, канонический ключ координаты, позиция) и `appearance_combination_members` (участники по одному на ось). Импорт объявляет координату независимо от того, несёт ли она свойства; выгрузка обходит объявления, а не значения, и прикрепляет значения по общему ключу. Миграция `0005_appearance_declarations` переносит существующие координаты — те, по которым что-то переопределено; координата без свойств следа не оставила и появится при следующей заливке, о чём в миграции сказано. Попутно вскрылось, что объявления нельзя обновлять на месте: позиция уникальна в пределах appearance, и upsert сталкивался сам с собой при смене порядка — обе таблицы объявлений теперь переписываются целиком.

- [x] 13.6 Разобрано. Круг **сходится за одну итерацию**: повторный `push --apply` тем же выгруженным пакетом даёт 143 «без изменений» и 0 обновлённых. Значит 29 обновлений на первом обратном push — однократная нормализация хранимого состояния, а не потеря. Сравнение двух последовательных выгрузок: 142 конфигурации из 143 совпадают побайтово после сортировки ключей, расходятся две — `pagination-dots` горизонтальный и вертикальный. Расхождение точечное: первая выгрузка отдаёт кросс-осевые вариации `m.line` и `s.line`, которые несут только `binding` и `parent` и ни одного свойства, вторая их не отдаёт. Причина в модели: `style_combinations` хранит **значения**, и сочетание без единого переопределения следа не оставляет. Это тот же класс, что «объявленная ось без использованных значений», который мы сделали хранимым через `appearance_variation_values`, но для сочетаний равнозначной таблицы объявлений нет.

- [x] 13.5 Причина найдена и она была в выгрузке, а не в импорте. `combinationsByOwner` индексировался одним `styleId`, тогда как стиль принадлежит паре (ДС, ось) и потому общий для всех appearance дизайн-системы, а сочетание принадлежит appearance. Индекс раздавал чужие пересечения соседям: `embedded-chip-group-dense` получал ось `shape` от `chip-group`, а вместе с ней — ссылку на ось, которой не объявляет. Ключ стал `appearanceId:styleId`. Проверено живьём: выгрузка `sdds_serv` проходит целиком, 143 конфигурации записаны. Две попытки чинить это в импорте (объявление осей из `targets` в `declareAxes` и `upsertVariations`) оставлены — они верны сами по себе, но к этому случаю отношения не имели.

- [x] 13.4 `import-uikit-api-meta.sh` починен: обращения к несуществующему `/ds/component-states` переведены на `/ds/states`. Наборы состояний скрипту не нужны — он заводит словарь, а не значения, и это записано комментарием, чтобы следующий читатель не искал `state-sets`. Проверено на чистой базе: 62 компонента, 39 состояний, 1193 свойства и 1219 алиасов записаны; повторный прогон создаёт ноль строк, то есть скрипт идемпотентен. В базе 83 компонента, 1540 свойств, 7 состояний взаимодействия и 39 объявленных компонентами, дублей по канону нет.

- [x] 13.3 Копирование объявлений осей в админке восстановлено. Заведена вложенная ручка `GET /ds/appearances/{id}/variations`: отдаёт оси в порядке `position` вместе с их значениями. Отдельная ручка, а не фильтрация общего списка на клиенте — иначе ради десятка строк пришлось бы возить все объявления базы. `copyBaseValues` переносит оси, их порядок, дефолт (через карту стилей, потому что в целевой ДС стиль свой) и объявленные значения вместе с `nativeId` и `nativeParent`. Кросс-осевые значения по-прежнему не копируются — они не копировались и до этого change'а, и предупреждение в комментарии переписано под фактическое положение дел. Ручка описана в OpenAPI, типы админки перегенерированы, добавлен тест.

- [x] 13.1 Разобрано. Префикс `screen-*` — не мусор в имени, а часть модели: legacy-ручка разбирает имя токена типографики как `screen.kind.size.weight` (`legacy.ts:668`), то есть экран принадлежит **имени токена**. Конфигурации же ссылаются без экрана (`body.l.normal`), значит одна ссылка обозначает три токена — по одному на `screen-s`, `screen-m`, `screen-l`, — и разрешить её в один `token_id` нельзя, не выбрав произвольно. Асимметрия с цветом сознательная: там режим (`dark`/`light`) — измерение **значения** (`token_values.mode`), а не имени, поэтому цвет резолвится. Последствие ограничено и измерено: текст значения сохраняется, круг остаётся неподвижной точкой (143 «без изменений»), а вывод типа не задет — типографика не paint. **Поправка к прежней формулировке:** нулевая доля у `shape` объяснялась не моделью, а моим загрузчиком токенов приёмки: он клал в имя листья атрибутов (`round.xs.kind`) вместо имени токена (`round.xs`).

- [x] 13.2 Проверено сборкой: `modal-bottom-sheet` вместо `bottom-sheet` в `meta.json` разрешается плагином **без** `META_NAME_ALIASES`. Прогон `:playground:theme-builder:generateComponents --rerun-tasks` на подменённой фикстуре проходит, **631 файл из 633 совпадает побайтово, ни один не различается**. Единственное расхождение — пара `ModalBottomSheet.kt`/`ModalBottomSheetStyles.kt` переезжает из пакета `styles/bottomsheet/` в `styles/modalbottomsheet/`: имя класса совпадает (алиас и `techToCamelCase` дают одно и то же `ModalBottomSheet`), а каталог пакета берётся из `componentName.lowercase()`, поэтому меняется путь импорта у потребителей темы. Алиас, таким образом, для разрешения имени не нужен, но имя пакета от выбора `bottom-sheet`/`modal-bottom-sheet` зависит. Push при этом по-прежнему отклоняет `bottom-sheet` из исходного пакета — в глобальном слое лежит `ModalBottomSheet` (результат 143 из 144 в 12.1). Фикстура `plasma-android` восстановлена, рабочее дерево того репозитория чисто.
