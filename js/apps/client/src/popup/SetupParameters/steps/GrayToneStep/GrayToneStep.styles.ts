import styled from 'styled-components';
import { IconArrowsMoveVertical } from '@salutejs/plasma-icons';

import { HoverSelect } from '../../../../features';
import { EditButton } from '../../../../components';

export const StyledHoverSelect = styled(HoverSelect)`
    margin: 3rem 0;
`;

export const StyledEditButton = styled(EditButton)`
    margin: 1.5rem 0;
`;

export const StyledIconArrowsMoveVertical = styled(IconArrowsMoveVertical)`
    width: 0.75rem !important;
    height: 0.75rem !important;
`;

